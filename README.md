python app.py lyrics.json background.jpg karaoke.mp4

Also, install ImageMagick (required for text rendering; download from https://imagemagick.org/).

Customization: Adjust fontsize, colors, positions, etc., in the script. For better Hebrew rendering, ensure your system font supports it well.